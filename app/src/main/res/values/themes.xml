<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Buscor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the background image drawable -->
        <item name="windowSplashScreenBackground">@color/black</item>
        <!-- Required attribute: points to your main app theme to use after loading -->
        <item name="postSplashScreenTheme">@style/Theme.Buscor</item>
    </style>

    <style name="Theme.Buscor" parent="Base.Theme.Buscor" />

</resources>